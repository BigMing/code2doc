# Code2Doc — 代码需求文档智能生成器 设计文档

> 本文档面向技术评审与工程竞赛，系统阐述项目的设计思路、技术架构、核心优势及非功能性设计考量。

---

## 一、项目概述

**Code2Doc** 是一款将工程源代码翻译为标准化需求文档，并自动补全规范注释的智能工具。用户粘贴源代码后，应用调用配置的 AI 模型进行深度分析，返回三份产物：

1. **一句话功能概括**（summary）
2. **结构化需求文档**（requirementDoc）— Markdown 格式，固定 7 章节结构
3. **补全注释后的代码**（annotatedCode）— 在类/方法定义、复杂算法、边界条件处添加标准注释块

前端采用三栏分屏工作区，支持同屏互操作与 Markdown/PDF 导出。

---

## 二、设计思路与架构

### 2.1 整体架构：客户端智能 + 服务端持久化

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器端                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ 输入面板     │  │ 文档预览面板 │  │ 代码/Diff 预览面板   │  │
│  │ CodeMirror   │  │ react-markdown│  │ react-syntax-highlighter │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│         │                  │                    │            │
│         └──────────────────┴────────────────────┘            │
│                        AppContext (React Context)            │
│                              │                               │
│              ┌───────────────┼───────────────┐               │
│              ▼               ▼               ▼               │
│        ┌─────────┐    ┌──────────┐    ┌──────────┐         │
│        │ ai-client│    │ storage  │    │  ai-config │         │
│        │ 工厂分发  │    │ 双写持久化│    │ 配置管理   │         │
│        └─────────┘    └──────────┘    └──────────┘         │
│                              │                               │
│                    ┌─────────┴─────────┐                     │
│                    ▼                   ▼                     │
│              localStorage          /api/data (Docker)        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Next.js API Route                       │
│                     /api/data (GET/POST/DELETE)              │
│                              │                               │
│                              ▼                               │
│                     服务器文件系统持久化                       │
│                     DATA_DIR=/app/data                       │
└─────────────────────────────────────────────────────────────┘
```

**架构核心决策**：
- **AI 调用发生在浏览器端**：利用用户本地网络直接连接各家 AI 提供商，不经过后端代理，降低服务端运维成本和延迟
- **双写持久化策略**：同时写入 `localStorage`（浏览器兼容）和 `/api/data` API（Docker 跨浏览器持久化），读取时优先服务端、失败回退本地
- **Standalone 输出模式**：Next.js 构建为 standalone，适合容器化一键部署

### 2.2 AI 调用层：统一工厂 + 协议适配

```
streamAnalyzeCode() / analyzeCode()
         │
         ▼
    createClient()
         │
    ┌────┴────┬─────────────┬──────────────┐
    ▼         ▼             ▼              ▼
GeminiClient OpenAiCompatibleClient  AnthropicClient
(@google/genai)   (fetch + SSE)         (fetch + SSE)
```

**设计要点**：
- 统一对外接口，内部按 `provider` 字段工厂分发
- Gemini 使用官方 SDK（支持 JSON Schema 约束输出）
- OpenAI 兼容类覆盖 OpenAI / 千问 / GLM / DeepSeek / Kimi / 私有化部署
- Anthropic 独立 SSE 协议解析
- 流式/非流式输出统一抽象，新增**流式开关**让用户手动控制

### 2.3 流式处理链路

```
用户点击生成 → streamStart() 重置状态
       │
       ▼
streamAnalyzeCode() 异步生成器
       │
       ▼
逐 chunk yield { chunk, accumulated, usage? }
       │
       ▼
streamChunk() 更新累积文本 → 实时 Skeleton 占位
       │
       ▼
流结束 → extractJsonFromStream() 容错解析 JSON
       │
       ▼
streamEnd() 触发打字机效果
       │
       ▼
useTypewriter Hook 接管 doc-panel / code-panel 逐字渲染
```

**容错设计**：
- `stream-parser.ts` 四层提取策略：直接解析 → Markdown 代码块剥离 → 栈计数法匹配最外层大括号 → 正则兜底提取
- 新增 **Zod 运行时 Schema 校验**：对 AI 返回的 `summary`/`requirementDoc`/`annotatedCode` 做字段存在性校验，缺失时自动尝试字段名映射修复（如 `requirement_doc` → `requirementDoc`）
- 解析失败时回退到文本展示，不白屏

---

## 三、技术特点

### 3.1 现代前端技术栈

| 领域 | 技术选型 | 选型理由 |
|------|---------|---------|
| 框架 | Next.js 15 (App Router) | React 服务端组件、自动代码分割、Standalone 输出 |
| 语言 | TypeScript 5.9 (strict) | 编译期类型安全，零隐式 any |
| 样式 | Tailwind CSS v4 + PostCSS | 原子化 CSS，主题变量通过 CSS 自定义属性管理 |
| UI 组件 | shadcn/ui (base-nova) | 基于 `@base-ui/react` 原语，轻量无 Radix 依赖 |
| 动画 | motion (Framer Motion 继任) | 声明式动画，支持 React 19 |
| 代码编辑 | CodeMirror 6 | 轻量、可扩展、支持多语言语法高亮 |
| Markdown | react-markdown + remark-gfm + rehype-slug | GFM 支持、自动标题锚点 |
| 分屏 | react-resizable-panels | 可拖拽面板，支持持久化尺寸 |

### 3.2 AI 工程化实践

- **JSON Schema 约束**：Gemini 客户端使用 `responseSchema` 强制 AI 返回结构化 JSON，降低解析失败率
- **SSE 统一解析**：`sse-parser.ts` 抽象 SSE 流解析逻辑，消除 OpenAI/Anthropic 客户端的重复代码
- **Token 实时统计**：各客户端从流式响应中实时提取 usage 信息，汇总展示给用户
- **Prompt 工程**：系统提示词包含 CoT 思维链要求（6 步思考过程）+ Few-shot 示例 + 注释风格按语言定制（Javadoc/JSDoc/Google Docstring/Go/Rustdoc 等）

### 3.3 中间件钩子系统

```typescript
export interface AiMiddleware {
  name: string;
  beforeRequest?: (params: { provider: string; model: string; codeLength: number }) => void | Promise<void>;
  afterResponse?: (result: AnalysisResult, usage?: TokenUsage) => void | Promise<void>;
  onError?: (error: Error) => void | Promise<void>;
}
```

- 支持请求前预处理、响应后处理、错误拦截
- 为后续接入**请求缓存**、**A/B 测试**、**埋点监控**预留扩展点

---

## 四、核心优势

### 4.1 多模型自主配置

支持 8 家主流 AI 提供商，覆盖国内外公有云与私有化部署：

| 提供商 | 类型 | 接入方式 |
|--------|------|---------|
| Google Gemini | 官方 SDK | `@google/genai`，JSON Schema 约束 |
| OpenAI | OpenAI 兼容 | fetch + SSE |
| Anthropic Claude | 独立协议 | fetch + SSE，独立 message 格式 |
| 阿里通义千问 | OpenAI 兼容 | DashScope 兼容端点 |
| 智谱 GLM | OpenAI 兼容 | 支持自定义 Base URL |
| DeepSeek | OpenAI 兼容 | 支持自定义 Base URL |
| Moonshot Kimi | OpenAI 兼容 | 支持自定义 Base URL |
| 通义千问（私有化） | OpenAI 兼容 | 必填自定义 Base URL，Key 可选 |

**优势**：用户可根据代码复杂度、成本预算、数据合规要求灵活切换模型；私有化部署满足金融/政府行业的数据不出域要求。

### 4.2 Docker 一键部署与跨浏览器持久化

```bash
docker compose up -d --build
```

- 数据通过挂载卷 `./data:/app/data` 持久化
- 配置和历史记录同时保存到浏览器 localStorage 和服务器文件系统
- 换浏览器后数据不丢失
- `output: 'standalone'` 确保最小化运行时依赖

---

## 五、安全研发能力

### 5.1 运行时安全

| 措施 | 实现 |
|------|------|
| **AI 返回校验** | Zod Schema 校验 + 字段映射修复，防止恶意/畸形 JSON 导致 XSS 或应用崩溃 |
| **Error Boundary** | React 错误边界捕获渲染异常，避免整个应用白屏 |
| **API Key 加密存储** | `crypto.ts` 对 Key 进行 AES 加密后存入 localStorage，非明文暴露 |
| **输入长度限制** | 50K 字符上限预警，防止 Prompt 注入或 Token 超限攻击 |
| **URL 校验** | 自定义 API 地址时校验 URL 格式，防止 SSRF 风险 |

### 5.2 通信安全

- 所有 AI 调用均通过 HTTPS（浏览器原生 TLS）
- 服务端 `/api/data` 存储 API 无鉴权（设计为内网 Docker 部署），如需公网暴露可通过反向代理叠加 Basic Auth

### 5.3 代码安全

- TypeScript `strict: true`，编译期捕获类型错误
- 无 `dangerouslySetInnerHTML` 使用，`react-markdown` 默认转义 HTML
- 文件上传限制为文本类型（源码文件），拒绝可执行文件

---

## 六、兼容性

### 6.1 AI 模型兼容性

- **流式/非流式双模式**：部分私有化部署的 OpenAI 兼容接口不支持 SSE，用户可手动关闭流式输出
- **超时控制**：`fetchWithTimeout` 封装，120 秒无响应自动中断，防止请求挂死
- **错误分类处理**：429/401/500/网络错误分类提示，附带"下一步操作建议"（如"切换到备用模型"）
- **字段缺失容错**：AI 返回字段名不一致时（如 `requirement_doc`），自动映射修复

### 6.2 浏览器与设备兼容性

- **暗色/亮色双主题**：`next-themes` + Tailwind `dark:` 前缀，支持跟随系统
- **响应式布局**：三栏分屏在桌面端（≥768px）使用 `react-resizable-panels`，平板/手机端自动切换为 Tab 布局（专注模式进一步简化）
- **触摸友好**：按钮最小 44×44px 触摸区域（移动端标准）
- **hydration 修复**：`suppressHydrationWarning` + `mounted` 标志位避免暗色模式闪烁

### 6.3 部署兼容性

- **Standalone 模式**：`next.config.ts` 设置 `output: 'standalone'`，构建产物自包含
- **Node.js 22 Alpine**：Docker 镜像基于 `node:22-alpine`，体积小（~340MB）
- **CentOS/SELinux 兼容**：AGENTS.md 文档明确标注 SELinux 配置建议
- **HMR 可控**：支持 `DISABLE_HMR=true` 环境变量，防止 Agent 编辑时频繁刷新

---

## 七、易用性

### 7.1 零门槛上手

- **Onboarding 引导**：首次访问 4 步分步引导，SVG 遮罩挖孔高亮核心操作区
- **示例代码**：`Ctrl+K` 一键加载电商订单服务示例，降低首次使用认知成本
- **自动语言识别**：粘贴代码后 1 秒延迟自动识别编程语言，减少手动选择
- **文件上传**：支持 `.java/.py/.js/.ts/.go/.cpp/.rs` 等 15+ 种源码格式直接上传

### 7.2 操作效率

- **快捷键体系**：
  - `Ctrl+Enter`：生成需求文档
  - `Ctrl+K`：加载示例代码
- **历史记录管理**：最多 10 条，支持标题搜索、按语言筛选、一键加载
- **重新生成**：保留代码和配置，一键清空结果重试，支持换模型后重跑
- **批量导出**：支持 Markdown 原始文件导出 + PDF 打印导出

### 7.3 容错与引导

- **代码长度预警**：15K 字符黄色预警，50K 字符红色拦截并提示 Token 风险
- **配置状态可视化**：顶部栏实时显示当前模型提供商和模型名称，未配置时橙色警示
- **流式开关状态**：输入面板底部实时显示流式输出是否开启

---

## 八、交互体验

### 8.1 视觉设计

- **信息层级**：大量使用 `text-[10px]`/`text-[11px]` + `font-bold` + `uppercase` + `tracking-wider`，形成紧凑专业的工具感
- **色彩体系**：以 slate 为底色，blue 为主操作色，emerald 为成功态，amber 为警告态，red 为错误态
- **暗色模式**：全链路适配，包括 CodeMirror 主题、Syntax Highlighter、Diff Viewer、shadcn 组件

### 8.2 动效与反馈

- **打字机效果**：`useTypewriter` Hook 智能加速——内容 <1000 字 15ms/字，>5000 字 3ms/字，整体控制在 3~5 秒
- **流式 Skeleton**：AI 响应过程中展示脉冲骨架屏，减少等待焦虑
- **生成进度条**：底部显示"连接→分析→解析→渲染"四步进度，当前步骤带旋转动画
- **Toast 通知**：操作成功/失败即时反馈（保存、导出、配置变更）
- **专注模式**：一键隐藏所有非核心 UI，进入沉浸式工作态

### 8.3 面板交互

- **可拖拽分屏**：`react-resizable-panels` 实现三栏自由调整，拖拽条 hover 高亮为蓝色
- **悬浮目录**：需求文档面板 hover 显示快速跳转目录，支持二级标题缩进
- **Diff 对比**：右栏支持分栏/内联两种 Diff 视图，高亮增删改

---

## 九、扩展能力

### 9.1 架构扩展性

- **AI Client 工厂模式**：新增提供商只需实现统一接口，注册到工厂即可
- **中间件钩子**：请求前/响应后/错误拦截三层钩子，方便接入缓存、埋点、限流
- **存储层抽象**：当前为 `localStorage + 文件系统` 双写，可扩展为 Redis/PostgreSQL 只需替换 `server-storage.ts`
- **Prompt 模板化**：系统提示词为字符串常量，未来可扩展为模板变量系统，支持用户自定义章节

### 9.2 功能扩展方向

| 方向 | 可行性 | 说明 |
|------|--------|------|
| 多文件/项目级分析 | 高 | 扩展文件上传为 ZIP/文件夹，AI 自动识别入口和依赖 |
| 需求 → 代码反向生成 | 中 | 基于现有 Prompt 体系，反向构造"文档 → 代码骨架"流程 |
| AST 可视化调用图 | 中 | 接入 `tree-sitter` 解析代码，生成 Mermaid 调用关系图 |
| 模型竞技场 | 高 | 并发请求两个模型，左右分栏对比结果质量 |
| 团队协作 | 中 | 基于 `/api/data` 扩展用户隔离、分享链接、评论审批 |
| 自定义模板市场 | 高 | Prompt Template 增加模板变量，支持保存/加载自定义模板 |

---

## 十、非功能性需求

### 10.1 性能

- **构建优化**：Next.js 自动代码分割，页面级懒加载
- **流式渲染**：AI 响应实时展示，首字节时间（TTFB）感知为 0
- **打字机加速**：长文本动态加速，避免用户等待过久
- **CodeMirror 轻量**：仅加载所需语言包和主题，按需扩展

### 10.2 可靠性

- **请求超时**：120 秒自动中断，防止资源耗尽
- **取消机制**：`AbortController` 支持用户随时中断生成
- **错误降级**：AI 返回异常时回退到文本展示，保证可用性
- **数据持久化**：双写策略确保单点故障不丢数据

### 10.3 可维护性

- **TypeScript 严格模式**：全链路类型安全
- **组件化设计**：每个面板独立，职责单一
- **测试覆盖**：42 个单元测试覆盖核心解析、校验、存储逻辑
- **迭代标记**：代码注释使用 `[第N轮新增]` / `[Docker 部署新增]` / `[修复]` 标记，便于追溯

### 10.4 可观测性

- **结构化日志**：工作日志按 `info`/`success`/`error`/`warning` 分级，上限 50 条自动滚动
- **Token 用量统计**：每次分析展示输入/输出/总 Token 数
- **原始报文查看**：支持查看原始请求/响应 JSON，便于调试

---

## 十一、总结

Code2Doc 的设计核心可以概括为：**"专业工具感 + 工程健壮性 + 极致兼容性"**。

- **专业工具感**：三栏分屏、紧凑信息密度、暗色模式、CodeMirror 语法高亮、PDF 导出——每一个细节都面向开发者日常工作的真实场景
- **工程健壮性**：Zod 运行时校验、Error Boundary、超时控制、双写持久化、42 个单元测试——拒绝"Demo 可用、生产崩溃"
- **极致兼容性**：8 家模型、公有云/私有化、流式/非流式、Docker 一键部署——让任何团队都能找到适合自己的部署方式

这套架构不仅满足当前"代码 → 文档"的核心场景，更为未来的"多文件分析"、"团队协作"、"模型竞技场"等高级功能预留了清晰的扩展路径。
