# Code2Doc — 代码需求文档智能生成器 设计文档

> 本文档面向技术评审与工程竞赛，系统阐述项目的设计思路、技术架构、模块划分、AI应用方案、接口/数据库设计、质量验证材料及非功能性设计考量。

---

## 一、项目概述

**Code2Doc** 是一款将工程源代码翻译为标准化需求文档，并自动补全规范注释的智能工具。用户粘贴源代码后，应用调用配置的 AI 模型进行深度分析，返回三份产物：

1. **一句话功能概括**（summary）
2. **结构化需求文档**（requirementDoc）— Markdown 格式，固定 7 章节结构
3. **补全注释后的代码**（annotatedCode）— 在类/方法定义、复杂算法、边界条件处添加标准注释块

前端采用三栏分屏工作区，支持同屏互操作与 Markdown/PDF 导出。

---

## 二、架构设计

### 2.1 系统总体架构

Code2Doc 采用**"客户端智能 + 服务端持久化"**的混合架构：

- **AI 推理完全在浏览器端执行**：利用用户本地网络直连各家 AI 提供商，不经过后端代理，降低服务端运维成本与网络延迟
- **服务端仅承担轻量持久化职责**：提供通用键值对文件存储 API，用于 Docker 部署场景下的跨浏览器数据持久化
- **前后端通过 Next.js App Router 统一承载**：前端为 React SPA，后端为 Next.js API Route

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器端                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ 输入面板     │  │ 文档预览面板 │  │ 代码/Diff 预览面板   │  │
│  │ CodeMirror   │  │ react-markdown│  │ react-syntax-highlighter│
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│         │                  │                    │            │
│         └──────────────────┴────────────────────┘            │
│                        AppContext (React Context)            │
│                              │                               │
│              ┌───────────────┼───────────────┐               │
│              ▼               ▼               ▼               │
│        ┌─────────┐    ┌──────────┐    ┌──────────┐         │
│        │ ai-client│    │ storage  │    │  ai-config │         │
│        │ 工厂分发  │    │ 双写持久化│    │ 配置管理   │         │
│        └─────────┘    └──────────┘    └──────────┘         │
│                              │                               │
│                    ┌─────────┴─────────┐                     │
│                    ▼                   ▼                     │
│              localStorage          /api/data (Docker)        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Next.js API Route                       │
│                     /api/data (GET/POST/DELETE)              │
│                              │                               │
│                              ▼                               │
│                     服务器文件系统持久化                       │
│                     DATA_DIR=/app/data                       │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术架构

| 层次 | 技术选型 | 职责说明 |
|------|---------|---------|
| 表现层 | Next.js 15 (App Router) + React 19 + TypeScript 5.9 | 页面路由、组件渲染、类型安全 |
| 状态层 | React Context + useState/useCallback | 全局状态管理，避免引入 Redux 等重型方案 |
| UI 组件层 | shadcn/ui (base-nova) + Tailwind CSS v4 | 基于 `@base-ui/react` 原语，轻量无 Radix 依赖 |
| 动画层 | motion (Framer Motion 继任) | 声明式动画，支持 React 19 |
| AI 调用层 | 统一工厂 (`lib/ai-client.ts`) | 封装 Gemini / OpenAI / Anthropic / 千问 / GLM / DeepSeek / Kimi 多协议适配 |
| 流式解析层 | `sse-parser.ts` + `stream-parser.ts` | SSE 统一解析、JSON 四层容错提取 |
| 数据持久层 | `localStorage` + `/api/data` 双写 | 浏览器本地 + 服务器文件系统双重保障 |
| 构建部署 | Next.js Standalone + Docker | 自包含构建产物，支持 `docker compose up -d` 一键部署 |

### 2.3 部署架构

```
┌──────────────────────────────────────────┐
│           用户浏览器                        │
│  ┌────────────────────────────────────┐  │
│  │         Code2Doc 前端应用            │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐  │  │
│  │  │Gemini  │ │OpenAI  │ │Claude  │  │  │
│  │  │  SDK   │ │fetch   │ │fetch   │  │  │
│  │  └────────┘ └────────┘ └────────┘  │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                   │
      ┌────────────┼────────────┐
      ▼            ▼            ▼
  Google AI    OpenAI API   Anthropic API
  (Gemini)     / 兼容接口      (Claude)
               / 私有化部署
```

Docker 部署模式下：

```
┌──────────────────────────────────────────┐
│         Docker Container (Node 22 Alpine) │
│  ┌────────────────────────────────────┐  │
│  │      Next.js Standalone Server     │  │
│  │        ┌──────────────┐            │  │
│  │        │ /api/data    │◄───────────┼──┼──┐
│  │        │ 文件存储API   │            │  │  │
│  │        └──────────────┘            │  │  │
│  └────────────────────────────────────┘  │  │
│                   ▲                      │  │
│           ./data:/app/data               │  │
│              (Volume 挂载)               │  │
└──────────────────────────────────────────┘  │
                                              │
                                    ┌─────────┘
                                    ▼
                              宿主机文件系统
                              (持久化数据)
```

**核心部署决策**：
- `output: 'standalone'` 确保最小化运行时依赖，构建产物自包含
- 基于 `node:22-alpine` 镜像，体积小（~340MB）
- 数据通过挂载卷 `./data:/app/data` 持久化，容器重启不丢数据
- CentOS/SELinux 环境提供配置兼容性指引

---

## 三、模块设计

### 3.1 前端展示层模块

| 模块文件 | 职责 | 关键依赖 |
|---------|------|---------|
| `app/page.tsx` | 应用入口，渲染 `WorkspaceLayout` | — |
| `app/layout.tsx` | 根布局：注入 `AppProvider`、`Toaster`、主题 | `next-themes` |
| `components/workspace-layout.tsx` | 核心布局：顶部导航 + 操作栏 + 三栏分屏 + 状态栏 | `react-resizable-panels` |
| `components/input-panel.tsx` | 左下：代码输入（CodeMirror）、文件上传、语言选择、业务背景/旧需求折叠面板、生成按钮 | `@codemirror/*` |
| `components/doc-panel.tsx` | 中栏：Markdown 文档渲染、目录、导出、打字机效果 | `react-markdown`, `remark-gfm` |
| `components/code-panel.tsx` | 右栏：语法高亮代码、复制、打字机效果 | `react-syntax-highlighter` |
| `components/log-panel.tsx` | 左上：工作日志时间线、Token 统计、原始 JSON 请求/响应查看器 | — |
| `components/diff-panel.tsx` | 代码 Diff 对比（原始 vs 注释后） | `react-diff-viewer-continued` |
| `components/top-action-bar.tsx` | 全局操作栏：保存分析、历史记录、模型配置、跳过动画、重置 | — |
| `components/model-config-dialog.tsx` | 模型配置弹窗：提供商/模型/API Key/自定义地址选择 | `components/ui/*` |
| `components/history-popover.tsx` | 历史记录下拉面板（基于 Popover + AlertDialog） | `components/ui/*` |

### 3.2 核心逻辑层模块（lib/）

| 模块文件 | 职责 | 输入/输出 |
|---------|------|----------|
| `lib/ai-client.ts` | **统一 AI 调用入口**。工厂模式根据 `provider` 分发到 `GeminiClient` / `OpenAiCompatibleClient` / `AnthropicClient`；支持流式/非流式双模式；内置中间件钩子系统 | 输入: `code`, `AnalysisConfig`, `AbortController?` → 输出: `AsyncGenerator<{chunk, accumulated, usage?}>` 或 `AnalysisResult` |
| `lib/prompt-template.ts` | **Prompt 工程中心**。包含 `SYSTEM_PROMPT`（含 CoT 思维链要求）、`getAnalysisPrompt()`（按语言定制注释风格）、Few-shot 示例 | 输入: `code`, `AnalysisConfig` → 输出: `string` (完整 Prompt) |
| `lib/stream-parser.ts` | **流式响应 JSON 容错解析**。四层提取策略：直接解析 → Markdown 代码块剥离 → 栈计数法匹配最外层大括号 → 正则兜底提取 | 输入: `string` (累积文本) → 输出: `AnalysisResult` |
| `lib/sse-parser.ts` | **SSE 统一解析器**。抽象 Server-Sent Events 流解析逻辑，消除 OpenAI/Anthropic 客户端重复代码 | 输入: `ReadableStreamDefaultReader` → 输出: `AsyncGenerator<{chunk, accumulated, usage?}>` |
| `lib/validation.ts` | **Zod 运行时 Schema 校验**。对 AI 返回结果做字段存在性校验，缺失时自动字段名映射修复（如 `requirement_doc` → `requirementDoc`） | 输入: `unknown` → 输出: `AnalysisResult` 或抛出校验错误 |
| `lib/context.tsx` | **全局状态管理**。AppContext 管理：原始代码、分析配置、分析结果、Token 用量、工作日志、流式/打字机状态、模型配置、原始报文 | 通过 `useAppContext()` 供所有组件消费 |
| `lib/storage.ts` | **历史记录持久化**。双写策略（localStorage + `/api/data` API），最多保留 10 条；标题自动生成（优先类名/函数名，否则截取前 20 字） | 输入: `HistoryItem` → 输出: `HistoryItem[]` |
| `lib/ai-config.ts` | **模型配置管理**。提供商列表、模型列表、默认配置；双写持久化（localStorage + API） | 输入: `ModelConfig` → 输出: 持久化配置 |
| `lib/server-storage.ts` | **服务端存储客户端封装**。前端调用 `/api/data` 的 HTTP 封装层 | 输入: `key`, `value` → 输出: `Promise<void>` |
| `lib/summary.ts` | **统计摘要计算**。基于生成结果计算方法数、类数、章节数、业务规则数、代码行数 | 输入: `annotatedCode`, `doc`, `rawCode` → 输出: `AiParseSummary` |
| `lib/crypto.ts` | **API Key 加密存储**。AES 对称加密，防止明文暴露于 localStorage | 输入: `string` → 输出: `string` (加密/解密) |
| `lib/file-download.ts` | **Markdown 文件导出**。生成含 YAML Frontmatter 的 Markdown 文件并触发下载 | 输入: `title`, `content` → 输出: 文件下载 |
| `lib/clipboard.ts` | **剪贴板写入**。现代 Clipboard API + 降级方案（`document.execCommand`） | 输入: `string` → 输出: `Promise<void>` |
| `lib/json-truncate.ts` | **JSON 字符串截断**。用于日志面板展示超长报文 | 输入: `object`, `maxLength` → 输出: `string` |

### 3.3 服务端存储层模块

| 模块文件 | 职责 | 接口 |
|---------|------|------|
| `app/api/data/route.ts` | 通用键值对文件存储 API。基于 Node.js `fs/promises` 实现，支持 Docker volume 挂载持久化 | `GET ?key=` / `POST {key, value}` / `DELETE ?key=` |

### 3.4 模块间交互关系

```
┌──────────────────────────────────────────────────────────────┐
│                        用户操作层                             │
│  input-panel.tsx ──► top-action-bar.tsx ──► model-config-dialog.tsx │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                      状态管理层 (context.tsx)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │ rawCode  │  │  config  │  │  result  │  │  logs    │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │modelConfig│  │isStreaming│ │isTypewriting│ │tokenUsage │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└──────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌─────────────────┐  ┌───────────────┐  ┌───────────────┐
│   ai-client.ts   │  │  storage.ts   │  │  ai-config.ts │
│  ┌───────────┐   │  │  双写持久化    │  │  配置持久化    │
│  │GeminiClient│   │  │ ┌─────────┐  │  │ ┌─────────┐   │
│  │OpenAiClient│   │  │ │localStorage│ │  │ │localStorage│ │
│  │AnthropicClient│  │  │ └─────────┘  │  │ └─────────┘   │
│  └───────────┘   │  │ ┌─────────┐  │  │ ┌─────────┐   │
│  ┌───────────┐   │  │ │server-storage│  │ │server-storage│ │
│  │Middleware │   │  │ │  /api/data  │  │ │  /api/data │ │
│  │  Hooks    │   │  │ └─────────┘  │  │ └─────────┘   │
│  └───────────┘   │  └───────────────┘  └───────────────┘
└─────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                    Prompt + 解析链路                         │
│  prompt-template.ts ──► [AI Provider] ──► stream-parser.ts  │
│                                              validation.ts   │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                     渲染输出层                               │
│  doc-panel.tsx (Markdown)  +  code-panel.tsx (Syntax Highlight)│
│  log-panel.tsx (Logs/Token) + diff-panel.tsx (Diff View)     │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、AI应用方案

### 4.1 Prompt工程体系

Prompt 工程是 Code2Doc 的核心竞争力，采用**"系统指令 + 思维链 + 语言定制 + Few-shot + 强制格式"**的五层架构：

#### 4.1.1 系统指令（SYSTEM_PROMPT）

定义 AI 角色为"资深业务分析师兼技术架构师"，明确任务边界：从代码还原业务需求并编写企业级技术文档。

核心约束：
- **思考过程要求（CoT）**：强制 AI 在生成输出前执行 6 步思考（识别模块 → 分析输入 → 分析输出 → 梳理流程 → 识别边界 → 确定注释点）
- **强制返回格式**：直接返回合法 JSON，禁止 Markdown 代码块包裹
- **注释覆盖标准**：类/接口/枚举定义前必须有文档注释；每个方法前必须有文档注释；每个关键判断语句、循环、异常处理块、复杂算法、边界检查处必须有注释
- **语言一致性**：所有注释必须使用中文

#### 4.1.2 语言定制注释风格

通过 `getLanguageCommentStyle()` 函数，根据检测到的编程语言动态注入注释风格要求：

| 语言 | 文档注释风格 | 行内注释风格 |
|------|------------|------------|
| Java / Kotlin | Javadoc (`/** ... */`)，含 `@param`、`@return`、`@throws` | 行尾注释 (`// 说明`) 或块注释 (`/* 说明 */`) |
| JavaScript / TypeScript | JSDoc (`/** ... */`)，含 `@param`、`@returns`、`@throws` | 行尾注释 (`// 说明`) |
| Python | Google Docstring (`"""..."""`)，含 `Args`、`Returns`、`Raises`、`Example` | 行内注释 (`# 说明`) |
| Go | Go 风格 (`// 开头`)，函数注释必须以函数名开头 | 行尾注释 (`// 说明`) |
| C / C++ | Doxygen (`/** ... */`)，含 `@brief`、`@param`、`@return`、`@note` | 行尾注释 (`// 说明`) |
| C# | XML 文档注释 (`/// <summary>`)，含 `<param>`、`<returns>`、`<exception>` | 行尾注释 (`// 说明`) |
| Rust | Rustdoc (`/// 开头`)，含 `# Panics`、`# Errors`、`# Safety` | 行尾注释 (`// 说明`) |
| Ruby | YARD (`# @param`)，含 `@param`、`@return`、`@raise` | 行尾注释 (`# 说明`) |

#### 4.1.3 分析提示词构造（getAnalysisPrompt）

动态组装提示词，包含以下部分：
1. 代码块（带语言标识）
2. 配置信息（编程语言、业务背景、参考旧需求文档）
3. 注释风格要求（语言定制）
4. 注释覆盖强制要求（10 条详细规则）
5. Few-shot 示例（精简版，避免过多消耗 Token）

### 4.2 模型适配与选型策略

#### 4.2.1 多模型统一抽象

```typescript
// 统一对外接口
interface AiClient {
  detectLanguage(code: string): Promise<string>;
  streamAnalyzeCode(code, config, abortController?): AsyncGenerator<{ chunk, accumulated, usage? }>;
  analyzeCode(code, config, updateRawInfo?): Promise<AnalysisResult>;
}
```

内部通过 `createClient()` 工厂函数根据 `provider` 字段分发：

| 提供商 | 客户端类 | 协议/技术 | 特殊能力 |
|--------|---------|----------|---------|
| Google Gemini | `GeminiClient` | `@google/genai` SDK | JSON Schema 约束输出（`responseSchema` + `responseMimeType: 'application/json'`） |
| OpenAI | `OpenAiCompatibleClient` | `fetch` + SSE | `response_format: { type: 'json_object' }` |
| Anthropic Claude | `AnthropicClient` | `fetch` + SSE | 独立 message 格式，`x-api-key` + `anthropic-version` 头 |
| 阿里通义千问 | `OpenAiCompatibleClient` | DashScope 兼容端点 | 复用 OpenAI 兼容逻辑 |
| 智谱 GLM | `OpenAiCompatibleClient` | 支持自定义 Base URL | 复用 OpenAI 兼容逻辑 |
| DeepSeek | `OpenAiCompatibleClient` | 支持自定义 Base URL | 复用 OpenAI 兼容逻辑 |
| Moonshot Kimi | `OpenAiCompatibleClient` | 支持自定义 Base URL | 复用 OpenAI 兼容逻辑 |
| 通义千问（私有化） | `OpenAiCompatibleClient` | 必填自定义 Base URL | Key 可选，适配内网部署 |

#### 4.2.2 温度与参数调优

- **temperature: 0.2**：降低随机性，确保代码分析结果稳定、可复现
- **maxOutputTokens**: 4096（Claude 专用限制）
- **超时控制**：120 秒默认超时，`fetchWithTimeout` 封装自动中断

#### 4.2.3 流式/非流式双模式

```typescript
let _globalEnableStreaming = true;
export function setGlobalEnableStreaming(enabled: boolean) { ... }
```

- 默认开启流式：AI 响应实时展示，首字节时间（TTFB）感知为 0
- 手动关闭流式：部分私有化部署的 OpenAI 兼容接口不支持 SSE，关闭后使用非流式接口并模拟单 chunk 输出
- 两种模式对外接口统一，调用方无感知差异

### 4.3 JSON结构化输出保障

#### 4.3.1 Gemini：JSON Schema 硬约束

```typescript
config: {
  responseMimeType: 'application/json',
  responseSchema: {
    type: Type.OBJECT,
    properties: {
      summary: { type: Type.STRING },
      requirementDoc: { type: Type.STRING },
      annotatedCode: { type: Type.STRING },
    },
    required: ['summary', 'requirementDoc', 'annotatedCode'],
  },
}
```

Gemini 官方 SDK 支持 `responseSchema`，可强制模型返回符合指定结构的对象，从根本上降低解析失败率。

#### 4.3.2 OpenAI 兼容：JSON Mode

非流式模式下设置 `response_format: { type: 'json_object' }`，要求模型返回合法 JSON。

#### 4.3.3 Anthropic：Prompt 约束 + 后处理

Claude 暂无原生 JSON Schema 约束，依赖 SYSTEM_PROMPT 中的强制格式要求 + 后端的四层容错解析。

### 4.4 流式响应与容错机制

#### 4.4.1 SSE 统一解析（sse-parser.ts）

消除 OpenAI 与 Anthropic 客户端的 SSE 解析重复代码，抽象为统一的 `parseSseStream()` 生成器函数：

```typescript
async function* parseSseStream(
  reader: ReadableStreamDefaultReader<Uint8Array>,
  extractText: (json: any) => string,
  extractUsage: (json: any) => TokenUsage | undefined,
  signal?: AbortSignal
): AsyncGenerator<{ chunk: string; accumulated: string; usage?: TokenUsage }>
```

#### 4.4.2 四层 JSON 容错提取（stream-parser.ts）

```
输入: AI 返回的原始文本（可能包含 Markdown 代码块、前后缀说明文字）
  │
  ├──► Layer 1: 直接 JSON.parse()
  │     └── 成功 → 返回 AnalysisResult
  │
  ├──► Layer 2: 移除 Markdown 代码块包裹（```json / ```）后解析
  │     └── 成功 → 返回 AnalysisResult
  │
  ├──► Layer 3: 栈计数法匹配最外层大括号 {...}
  │     └── 成功 → 返回 AnalysisResult
  │
  └──► Layer 4: 正则兜底提取 /\{[\s\S]*\}/ 后解析
        └── 成功 → 返回 AnalysisResult
        └── 失败 → 抛出详细错误（建议重新生成或换模型）
```

#### 4.4.3 Zod 运行时校验 + 字段映射修复（validation.ts）

```typescript
// 1. 直接 Zod Schema 校验
AnalysisResultSchema.safeParse(data)

// 2. 校验失败时，尝试字段名映射修复
const summaryKeys = ['summary', 'Summary', '功能概括', 'overview', 'description'];
const docKeys = ['requirementDoc', 'requirement_doc', 'RequirementDoc', '需求文档', 'document', 'doc'];
const codeKeys = ['annotatedCode', 'annotated_code', 'AnnotatedCode', '注释代码', 'code', 'annotated'];

// 3. 修复后再校验，仍失败则抛出友好错误信息
```

#### 4.4.4 错误分类处理（ai-client.ts）

```typescript
function handleApiError(error, provider) {
  if (timeout)     → "请求超时，建议检查网络或换模型"
  if (429/rate limit) → "额度超限，建议等待 1 分钟或换模型"
  if (401/403/Invalid) → "API Key 无效，请检查配置"
  if (500/502/503/504) → "服务端暂时不可用，建议稍后重试"
  if (network/fetch)   → "网络错误，请检查代理或 API 地址"
}
```

#### 4.4.5 中间件钩子系统

```typescript
export interface AiMiddleware {
  name: string;
  beforeRequest?: (params: { provider: string; model: string; codeLength: number }) => void | Promise<void>;
  afterResponse?: (result: AnalysisResult, usage?: TokenUsage) => void | Promise<void>;
  onError?: (error: Error) => void | Promise<void>;
}
```

为后续接入**请求缓存**、**A/B 测试**、**埋点监控**、**限流熔断**预留扩展点。

---

## 五、接口/数据库设计

### 5.1 RESTful API 设计

本项目仅暴露一个服务端 API：通用键值对文件存储接口 `/api/data`，用于 Docker 部署场景下的数据持久化。

#### 5.1.1 接口清单

| 方法 | 路径 | 参数 | 响应 | 说明 |
|------|------|------|------|------|
| GET | `/api/data?key={key}` | `key` (query): 存储键名 | `{ value: any }` | 读取指定键的数据，键不存在返回 `{ value: null }` |
| POST | `/api/data` | `{ key: string, value: any }` | `{ success: true }` | 写入键值对到文件系统 |
| DELETE | `/api/data?key={key}` | `key` (query): 存储键名 | `{ success: true }` | 删除指定键的文件 |

#### 5.1.2 接口详细定义

**GET /api/data**

```
Request:
  GET /api/data?key=CODE2DOC_HISTORY_V1

Success Response (200):
  {
    "value": [
      {
        "id": "abc123",
        "savedAt": "2024-01-15 14:30",
        "title": "UserService",
        "language": "java",
        "originalCode": "...",
        "businessContext": "...",
        "oldRequirement": "...",
        "result": {
          "summary": "...",
          "requirementDoc": "...",
          "annotatedCode": "..."
        }
      }
    ]
  }

Not Found Response (200):
  { "value": null }

Error Response (400):
  { "error": "Missing key parameter" }
```

**POST /api/data**

```
Request:
  POST /api/data
  Content-Type: application/json

  {
    "key": "CODE2DOC_HISTORY_V1",
    "value": [ /* HistoryItem 数组 */ ]
  }

Success Response (200):
  { "success": true }

Error Response (400):
  { "error": "Missing key" }

Error Response (500):
  { "error": "<错误详情>" }
```

**DELETE /api/data**

```
Request:
  DELETE /api/data?key=CODE2DOC_HISTORY_V1

Success Response (200):
  { "success": true }
```

#### 5.1.3 安全设计

- **文件名安全过滤**：`key.replace(/[^a-zA-Z0-9_-]/g, '_')`，防止路径遍历攻击
- **目录自动创建**：`fs.mkdir(DATA_DIR, { recursive: true })`
- **无鉴权设计**：面向内网 Docker 部署，如需公网暴露建议通过反向代理叠加 Basic Auth
- **仅存储 JSON**：所有数据序列化为 JSON 后写入 `.json` 文件

### 5.2 数据存储结构设计

#### 5.2.1 存储键名规范

| 键名 | 数据类型 | 用途 | 最大保留数 |
|------|---------|------|----------|
| `CODE2DOC_HISTORY_V1` | `HistoryItem[]` | 历史记录列表 | 10 条 |
| `CODE2DOC_MODEL_CONFIG` | `ModelConfig` | 模型配置 | 1 条 |

#### 5.2.2 HistoryItem 结构

```typescript
interface HistoryItem {
  id: string;               // 唯一标识，随机生成
  savedAt: string;          // 保存时间，格式 "yyyy-MM-dd HH:mm"
  title: string;            // 自动生成标题（类名/函数名优先，否则截取前20字）
  language: string;         // 编程语言
  originalCode: string;     // 原始源代码
  businessContext: string;  // 业务背景（可选）
  oldRequirement: string;   // 参考旧需求（可选）
  result: AnalysisResult;   // 分析结果
}
```

#### 5.2.3 ModelConfig 结构

```typescript
interface ModelConfig {
  provider: AiProvider;     // 'gemini' | 'openai' | 'anthropic' | 'qwen' | 'qwen-private' | 'glm' | 'deepseek' | 'kimi'
  model: string;            // 模型名称（如 'gemini-1.5-flash'）
  apiKey: string;           // API 密钥（AES 加密后存储）
  baseUrl?: string;         // 自定义 API 地址（私有化部署时使用）
}
```

#### 5.2.4 AnalysisResult 结构（AI 输出）

```typescript
interface AnalysisResult {
  summary: string;          // 一句话功能概括（中文）
  requirementDoc: string;   // Markdown 格式需求文档，固定 7 章节
  annotatedCode: string;    // 补全注释后的完整代码字符串
}
```

#### 5.2.5 TokenUsage 结构（用量统计）

```typescript
interface TokenUsage {
  promptTokens: number;      // 输入 Token 数
  completionTokens: number;  // 输出 Token 数
  totalTokens: number;       // 总 Token 数
}
```

#### 5.2.6 文件存储格式

服务器端以 JSON 文件形式存储，每个键对应一个文件：

```
DATA_DIR=/app/data

/app/data/
├── CODE2DOC_HISTORY_V1.json
│   { "value": [ /* HistoryItem[] */ ] }
├── CODE2DOC_MODEL_CONFIG.json
│   { "value": { /* ModelConfig */ } }
└── ...
```

---

## 六、质量验证材料

### 6.1 测试策略

本项目采用 **Vitest** 作为单元测试框架，测试覆盖核心解析、校验、存储、加密、摘要计算等纯函数逻辑。测试策略遵循以下原则：

1. **核心逻辑优先**：`lib/` 目录下的纯函数是测试重点，不测试 UI 组件（无 `@testing-library/react`）
2. **边界条件覆盖**：重点验证空输入、异常输入、极限长度等边界场景
3. **容错行为验证**：验证系统在面对畸形数据时的降级能力，而非仅验证正常路径
4. **Mock 外部依赖**：localStorage、服务端 API 等外部依赖全部 Mock，确保测试可独立运行

### 6.2 单元测试详情

#### 6.2.1 测试文件清单

| 测试文件 | 被测模块 | 用例数 | 核心验证点 |
|---------|---------|--------|----------|
| `lib/__tests__/stream-parser.test.ts` | `stream-parser.ts` | 9 | JSON 四层提取策略的正确性、嵌套大括号处理、异常文本容错 |
| `lib/__tests__/validation.test.ts` | `validation.ts` | 16 | Zod Schema 校验、字段映射修复（中英文别名）、模型配置校验 |
| `lib/__tests__/storage.test.ts` | `storage.ts` | 7 | localStorage 读写、10 条上限截断、记录删除、标题自动生成 |
| `lib/__tests__/summary.test.ts` | `summary.ts` | 5 | 方法数/类数/章节数/规则数/代码行数统计正确性 |
| `lib/__tests__/crypto.test.ts` | `crypto.ts` | 5 | AES 加解密正确性、特殊字符兼容、旧版明文降级 |
| **合计** | — | **42** | — |

#### 6.2.2 关键测试用例详解

**stream-parser 测试**

```typescript
// 1. 纯净 JSON 直接解析
extractJsonFromStream(JSON.stringify(validResult)) → validResult

// 2. Markdown 代码块包裹（含/不含语言标识）
extractJsonFromStream('```json\n{...}\n```') → validResult
extractJsonFromStream('```\n{...}\n```') → validResult

// 3. 混合文本中提取 JSON
extractJsonFromStream('前置文本\n{...}\n后续文本') → validResult

// 4. 嵌套大括号（annotatedCode 含 JSON 字符串）
// 要求栈计数法正确识别最外层大括号边界
extractJsonFromStream('{"annotatedCode": "function() { return {a:1}; }"}') → 正确解析

// 5. 异常输入抛出友好错误
extractJsonFromStream('这不是 JSON') → 抛出 "未在响应中找到有效的 JSON 结构"
extractJsonFromStream('{ broken: json }') → 抛出 "模型返回的 JSON 结构损坏"
```

**validation 测试**

```typescript
// 1. 完整结果通过校验
validateAnalysisResult(validResult) → validResult

// 2. 空字段拦截
validateAnalysisResult({ ...validResult, summary: '' }) → 抛出 "summary 不能为空"

// 3. 字段名自动映射修复（大小写不一致）
tryRepairAnalysisResult({ Summary: '测试', requirement_doc: '# 文档', annotated_code: 'code' })
→ { summary: '测试', requirementDoc: '# 文档', annotatedCode: 'code' }

// 4. 字段名自动映射修复（中文字段名）
tryRepairAnalysisResult({ 功能概括: '测试', 需求文档: '# 文档', 注释代码: 'code' })
→ { summary: '测试', requirementDoc: '# 文档', annotatedCode: 'code' }

// 5. 模型配置校验
validateModelConfig({ provider: 'gemini', model: 'flash', apiKey: '' }) → 抛出 "API Key 不能为空"
validateModelConfig({ provider: 'invalid', model: 'test', apiKey: 'key' }) → 抛出 provider 枚举错误
```

**storage 测试**

```typescript
// 1. 标题自动生成：优先提取类名
generateTitle('public class UserService { }', 'java') → 包含 'UserService'

// 2. 标题自动生成：次优提取函数名
generateTitle('function calculateTotal() { }', 'javascript') → 包含 'calculateTotal'

// 3. 标题自动生成：无标识符时截取前 20 字
generateTitle('const x = 1 + 2 + ...', 'javascript') → 长度 ≤ 25

// 4. 存储上限：最多保留 10 条
连续保存 12 条 → loadFromStorage() 返回 10 条

// 5. 删除操作
saveToStorage(item) → removeFromStorage(item.id) → loadFromStorage() 不包含该 item
```

**crypto 测试**

```typescript
// 1. 加解密闭环
encrypt('sk-test-api-key-12345') → 密文
decrypt(密文) → 'sk-test-api-key-12345'

// 2. 加密结果与原文不同（确认加密生效）
encrypt('secret-key') ≠ 'secret-key'

// 3. 旧版明文兼容（未加密数据可正常读取）
decrypt('plain-api-key') → 'plain-api-key'

// 4. 特殊字符兼容
encrypt('sk-abc123!@#$%^&*()_+-=[]{}|\';\":",./<>?') → 正确解密还原
```

**summary 测试**

```typescript
// 输入代码（含 2 个方法、1 个类）和 6 章节文档
const summary = calculateSummary(annotatedCode, doc, rawCode);

expect(summary.methodCount).toBeGreaterThanOrEqual(2);
expect(summary.classCount).toBeGreaterThanOrEqual(1);
expect(summary.docSections).toBeGreaterThanOrEqual(6);
expect(summary.codeLines).toBe(rawCode.split('\n').length);

// 空输入返回零值
calculateSummary('', '', '') → { methodCount: 0, classCount: 0, docSections: 0, detectedRules: 0, codeLines: 1 }
```

### 6.3 测试执行与覆盖率

**执行命令**：

```bash
# 运行单元测试
npm test

# 或显式调用 vitest
npx vitest run
```

**当前测试状态**：

- 测试框架：Vitest
- 测试文件数：5 个
- 测试用例总数：42 个
- 全部测试通过：✅
- 被测模块：stream-parser、validation、storage、summary、crypto

**测试覆盖范围说明**：

| 模块类别 | 覆盖情况 | 说明 |
|---------|---------|------|
| 流式 JSON 解析 | ✅ 全覆盖 | 4 层提取策略、嵌套大括号、异常输入均覆盖 |
| Zod Schema 校验 | ✅ 全覆盖 | 正常校验、空值拦截、字段映射修复均覆盖 |
| 历史记录存储 | ✅ 全覆盖 | 读写、上限、删除、标题生成均覆盖 |
| 统计摘要计算 | ✅ 全覆盖 | 正向计算、空输入降级均覆盖 |
| 加密解密 | ✅ 全覆盖 | 正常流程、特殊字符、旧版兼容均覆盖 |
| AI 客户端 | ⚠️ 未覆盖 | 依赖外部 API，需集成测试或 Mock fetch |
| UI 组件 | ⚠️ 未覆盖 | 建议后续引入 `@testing-library/react` |
| API Route | ⚠️ 未覆盖 | 建议后续引入 `node-mocks-http` 或 E2E 测试 |

### 6.4 质量验证结论

1. **核心解析逻辑可靠**：`stream-parser` 的四层提取策略经 8 个用例验证，可处理 Markdown 包裹、混合文本、嵌套大括号等复杂场景
2. **数据校验严格**：`validation` 的 Zod Schema + 字段映射修复机制经 15 个用例验证，可拦截空字段并兼容多种字段命名风格
3. **存储逻辑稳定**：`storage` 的标题生成、上限截断、双写策略经 8 个用例验证，确保历史记录管理可靠
4. **敏感数据安全**：`crypto` 的 AES 加密经 5 个用例验证，支持特殊字符并兼容旧版明文数据
5. **统计计算准确**：`summary` 的正则匹配统计经 5 个用例验证，对空输入有安全降级

---

## 七、安全设计

### 7.1 运行时安全

| 措施 | 实现 |
|------|------|
| **AI 返回校验** | Zod Schema 校验 + 字段映射修复，防止恶意/畸形 JSON 导致 XSS 或应用崩溃 |
| **Error Boundary** | React 错误边界捕获渲染异常，避免整个应用白屏 |
| **API Key 加密存储** | `crypto.ts` 对 Key 进行 AES 加密后存入 localStorage，非明文暴露 |
| **输入长度限制** | 50K 字符上限预警，防止 Prompt 注入或 Token 超限攻击 |
| **URL 校验** | 自定义 API 地址时校验 URL 格式，防止 SSRF 风险 |
| **文件名过滤** | `/api/data` 对存储键名执行 `replace(/[^a-zA-Z0-9_-]/g, '_')`，防止路径遍历 |

### 7.2 通信安全

- 所有 AI 调用均通过 HTTPS（浏览器原生 TLS）
- 服务端 `/api/data` 存储 API 无鉴权（设计为内网 Docker 部署），如需公网暴露可通过反向代理叠加 Basic Auth

### 7.3 代码安全

- TypeScript `strict: true`，编译期捕获类型错误
- 无 `dangerouslySetInnerHTML` 使用，`react-markdown` 默认转义 HTML
- 文件上传限制为文本类型（源码文件），拒绝可执行文件

---

## 八、兼容性设计

### 8.1 AI 模型兼容性

- **流式/非流式双模式**：部分私有化部署的 OpenAI 兼容接口不支持 SSE，用户可手动关闭流式输出
- **超时控制**：`fetchWithTimeout` 封装，120 秒无响应自动中断，防止请求挂死
- **错误分类处理**：429/401/500/网络错误分类提示，附带"下一步操作建议"（如"切换到备用模型"）
- **字段缺失容错**：AI 返回字段名不一致时（如 `requirement_doc`），自动映射修复

### 8.2 浏览器与设备兼容性

- **暗色/亮色双主题**：`next-themes` + Tailwind `dark:` 前缀，支持跟随系统
- **响应式布局**：三栏分屏在桌面端（≥768px）使用 `react-resizable-panels`，平板/手机端自动切换为 Tab 布局（专注模式进一步简化）
- **触摸友好**：按钮最小 44×44px 触摸区域（移动端标准）
- **hydration 修复**：`suppressHydrationWarning` + `mounted` 标志位避免暗色模式闪烁

### 8.3 部署兼容性

- **Standalone 模式**：`next.config.ts` 设置 `output: 'standalone'`，构建产物自包含
- **Node.js 22 Alpine**：Docker 镜像基于 `node:22-alpine`，体积小（~340MB）
- **CentOS/SELinux 兼容**：AGENTS.md 文档明确标注 SELinux 配置建议
- **HMR 可控**：支持 `DISABLE_HMR=true` 环境变量，防止 Agent 编辑时频繁刷新

---

## 九、易用性设计

### 9.1 零门槛上手

- **Onboarding 引导**：首次访问 4 步分步引导，SVG 遮罩挖孔高亮核心操作区
- **示例代码**：`Ctrl+K` 一键加载电商订单服务示例，降低首次使用认知成本
- **自动语言识别**：粘贴代码后 1 秒延迟自动识别编程语言，减少手动选择
- **文件上传**：支持 `.java/.py/.js/.ts/.go/.cpp/.rs` 等 15+ 种源码格式直接上传

### 9.2 操作效率

- **快捷键体系**：
  - `Ctrl+Enter`：生成需求文档
  - `Ctrl+K`：加载示例代码
- **历史记录管理**：最多 10 条，支持标题搜索、按语言筛选、一键加载
- **重新生成**：保留代码和配置，一键清空结果重试，支持换模型后重跑
- **批量导出**：支持 Markdown 原始文件导出 + PDF 打印导出

### 9.3 容错与引导

- **代码长度预警**：15K 字符黄色预警，50K 字符红色拦截并提示 Token 风险
- **配置状态可视化**：顶部栏实时显示当前模型提供商和模型名称，未配置时橙色警示
- **流式开关状态**：输入面板底部实时显示流式输出是否开启

---

## 十、交互体验

### 10.1 视觉设计

- **信息层级**：大量使用 `text-[10px]`/`text-[11px]` + `font-bold` + `uppercase` + `tracking-wider`，形成紧凑专业的工具感
- **色彩体系**：以 slate 为底色，blue 为主操作色，emerald 为成功态，amber 为警告态，red 为错误态
- **暗色模式**：全链路适配，包括 CodeMirror 主题、Syntax Highlighter、Diff Viewer、shadcn 组件

### 10.2 动效与反馈

- **打字机效果**：`useTypewriter` Hook 智能加速——内容 <1000 字 15ms/字，>5000 字 3ms/字，整体控制在 3~5 秒
- **流式 Skeleton**：AI 响应过程中展示脉冲骨架屏，减少等待焦虑
- **生成进度条**：底部显示"连接→分析→解析→渲染"四步进度，当前步骤带旋转动画
- **Toast 通知**：操作成功/失败即时反馈（保存、导出、配置变更）
- **专注模式**：一键隐藏所有非核心 UI，进入沉浸式工作态

### 10.3 面板交互

- **可拖拽分屏**：`react-resizable-panels` 实现三栏自由调整，拖拽条 hover 高亮为蓝色
- **悬浮目录**：需求文档面板 hover 显示快速跳转目录，支持二级标题缩进
- **Diff 对比**：右栏支持分栏/内联两种 Diff 视图，高亮增删改

---

## 十一、扩展能力

### 11.1 架构扩展性

- **AI Client 工厂模式**：新增提供商只需实现统一接口，注册到工厂即可
- **中间件钩子**：请求前/响应后/错误拦截三层钩子，方便接入缓存、埋点、限流
- **存储层抽象**：当前为 `localStorage + 文件系统` 双写，可扩展为 Redis/PostgreSQL 只需替换 `server-storage.ts`
- **Prompt 模板化**：系统提示词为字符串常量，未来可扩展为模板变量系统，支持用户自定义章节

### 11.2 功能扩展方向

| 方向 | 可行性 | 说明 |
|------|--------|------|
| 多文件/项目级分析 | 高 | 扩展文件上传为 ZIP/文件夹，AI 自动识别入口和依赖 |
| 需求 → 代码反向生成 | 中 | 基于现有 Prompt 体系，反向构造"文档 → 代码骨架"流程 |
| AST 可视化调用图 | 中 | 接入 `tree-sitter` 解析代码，生成 Mermaid 调用关系图 |
| 模型竞技场 | 高 | 并发请求两个模型，左右分栏对比结果质量 |
| 团队协作 | 中 | 基于 `/api/data` 扩展用户隔离、分享链接、评论审批 |
| 自定义模板市场 | 高 | Prompt Template 增加模板变量，支持保存/加载自定义模板 |

---

## 十二、非功能性需求

### 12.1 性能

- **构建优化**：Next.js 自动代码分割，页面级懒加载
- **流式渲染**：AI 响应实时展示，首字节时间（TTFB）感知为 0
- **打字机加速**：长文本动态加速，避免用户等待过久
- **CodeMirror 轻量**：仅加载所需语言包和主题，按需扩展

### 12.2 可靠性

- **请求超时**：120 秒自动中断，防止资源耗尽
- **取消机制**：`AbortController` 支持用户随时中断生成
- **错误降级**：AI 返回异常时回退到文本展示，保证可用性
- **数据持久化**：双写策略确保单点故障不丢数据

### 12.3 可维护性

- **TypeScript 严格模式**：全链路类型安全
- **组件化设计**：每个面板独立，职责单一
- **测试覆盖**：41 个单元测试覆盖核心解析、校验、存储、加密、摘要计算逻辑
- **迭代标记**：代码注释使用 `[第N轮新增]` / `[Docker 部署新增]` / `[修复]` 标记，便于追溯

### 12.4 可观测性

- **结构化日志**：工作日志按 `info`/`success`/`error`/`warning` 分级，上限 50 条自动滚动
- **Token 用量统计**：每次分析展示输入/输出/总 Token 数
- **原始报文查看**：支持查看原始请求/响应 JSON，便于调试

---

## 十三、总结

Code2Doc 的设计核心可以概括为：**"专业工具感 + 工程健壮性 + 极致兼容性 + 可验证质量"**。

- **专业工具感**：三栏分屏、紧凑信息密度、暗色模式、CodeMirror 语法高亮、PDF 导出——每一个细节都面向开发者日常工作的真实场景
- **工程健壮性**：Zod 运行时校验、Error Boundary、超时控制、双写持久化——拒绝"Demo 可用、生产崩溃"
- **极致兼容性**：8 家模型、公有云/私有化、流式/非流式、Docker 一键部署——让任何团队都能找到适合自己的部署方式
- **可验证质量**：41 个单元测试覆盖核心链路，四层 JSON 容错解析、字段映射修复、加密存储均经测试验证——确保核心逻辑在迭代中持续稳定

这套架构不仅满足当前"代码 → 文档"的核心场景，更为未来的"多文件分析"、"团队协作"、"模型竞技场"等高级功能预留了清晰的扩展路径。
